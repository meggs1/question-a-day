<% if @tag.errors.any? %>
  <%= render partial: 'layouts/errors', locals: {object: @tag}%>
<% end %>

<%= form_for @tag do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %><br>

    <%= f.submit %>
<% end %>